# Developer --patch overlay: relative source only. Stock install uses
# cordis.patch.yml via dsh.bundle, not this file.
- insert:
    - id: dsh-web-search-firecrawl
      name: './src/dsh-web-search-firecrawl.ts'
